[project]
name = "pynosqlc-mongodb"
version = "0.1.0"
description = "MongoDB driver for pynosqlc"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pynosqlc-core",
    "pymongo>=4.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pynosqlc"]

[tool.uv.sources]
pynosqlc-core = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
